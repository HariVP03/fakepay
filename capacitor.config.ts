import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.fakepay",
  appName: "FakePay",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
